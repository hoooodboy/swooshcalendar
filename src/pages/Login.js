import React from 'react';
import Red from '../login-components/Red';
import Template from '../components/Template';

const Login = () => {
    return (
        <Template>
        <Red/>
        </Template>
    );
}

export default Login;