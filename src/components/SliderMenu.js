import React from 'react';
import styled from 'styled-components';

const SliderMenuBlock = styled.div`
    flex: 1;
    background-color: red;
`;

const SliderMenu = () => {
    return (
        <SliderMenuBlock/>
    );
}

export default SliderMenu;